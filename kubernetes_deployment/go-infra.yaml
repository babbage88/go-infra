apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-infra
spec:
  replicas: 3
  selector:
    matchLabels:
      app: go-infra
  template:
    metadata:
      labels:
        app: go-infra
    spec:
      containers:
      - name: go-infra
        image: docker.io/jtrahan88/goinfra:v1.1.3
        ports:
        - containerPort: 8993
        command: ["/app/server"]
        args: ["--envfile", "/run/secrets/k3s.env"]
        env:
        - name: DOCKER_IMG_TAG
          value: "v1.1.3"
        volumeMounts:
        - name: cf-token-ini
          mountPath: "/run/secrets/cf_token.ini"
          subPath: cf_token.ini
        - name: k3s-env
          mountPath: "/run/secrets/k3s.env"
          subPath: k3s.env
      volumes:
      - name: cf-token-ini
        secret:
          secretName: cf-token-ini
      - name: k3s-env
        secret:
          secretName: k3s-env
      imagePullSecrets:
      - name: dockercred
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - go-infra
            topologyKey: "kubernetes.io/hostname"

